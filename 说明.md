# goutils 功能说明

## 1. 项目概览

本项目是一个 Go 语言常用工具库，主要面向基础数据处理、类型转换、校验、加密、随机数、ID 生成和时间处理等场景。模块划分清晰，适合在业务代码中直接复用。当前代码库的模块主要包括：

- anyto
- strto
- valid
- crypto
- rand
- id
- time

根目录的 `go.mod` 声明模块地址为 `github.com/lily0749labs/goutils`。

---

## 2. 模块结构

```text
goutils/
├── anyto/        # 任意类型转基础类型、数组转字符串
├── strto/        # 字符串与整型/无符号整型/布尔值/字节数组转换
├── valid/        # 各类数据合法性校验
├── crypto/       # 哈希、RSA、AES 等加密相关能力
├── rand/         # 随机数、随机字符串、概率判定、英文名生成
├── id/           # 雪花 ID、UUID Token
├── time/         # Carbon 时间封装与常用时间处理
├── go.mod        # 模块声明
└── go.sum        # 依赖锁定
```

---

## 3. 模块功能说明

### 3.1 anyto 包：任意类型转换工具

职责：将任意值转换为常见基础类型，适合接受 `interface{}` / `any` 的场景。

主要能力：

- `AnyToInt`、`AnyToInt8`、`AnyToInt16`、`AnyToInt32`、`AnyToInt64`
- `AnyToUint`、`AnyToUint8`、`AnyToUint16`、`AnyToUint32`、`AnyToUint64`
- `AnyToFloat32`、`AnyToFloat64`
- `AnyToBool`
- `AnyToStr`
- 数组转换：`ArrIntToStr`、`ArrUintToStr`、`ArrAnyToStr` 等

适用场景：

- API 接口参数转换
- 数据表单值统一转换
- JSON / map / 反射场景下的基础类型处理

---

### 3.2 strto 包：字符串与基础类型转换

职责：提供字符串到数字、布尔值、字节数组等内容的快速转换。

主要能力：

- `StrToInt` / `StrToInt8` / `StrToInt16` / `StrToInt32` / `StrToInt64`
- `StrToUint` / `StrToUint8` / `StrToUint16` / `StrToUint32` / `StrToUint64`
- `StrToBool`
- `StrToBytes`
- 数组转换：
  - `ArrStrToInt`
  - `ArrStrToUint`
  - `ArrStrToBool`

适用场景：

- 命令行参数转换
- 配置读取后的数据标准化
- 字符串型输入统一转为数值类型
- 处理文本内容和字节流

---

### 3.3 valid 包：数据合法性校验

职责：校验常见字符串格式和业务字段是否合法，适合表单、接口参数、实体字段检查。

主要能力：

- 日期时间校验：
  - `IsTime`
  - `IsDate`
  - `IsDateTime`
- 身份信息：
  - `IsIDCard`
  - `IsIDCard18`
  - `IsIDCard15`
- 联系方式：
  - `IsMobile`
  - `IsTelephone`
  - `IsPostalCode`
- 数字与金额：
  - `IsDecimal`
  - `IsNumber`
  - `IsBankCardNo`
- 中文/语言：
  - `IsAllChinese`
  - `IsContainChinese`
  - `IsChineseName`
  - `IsEnglishName`
  - `IsLanguage`（语言相关合法性）
- 网络/URL：
  - `IsEmail`
  - `IsIPv4`
  - `IsIPv6`
  - `IsURL`
  - `IsJSON`
- 社交账号：
  - `IsQQ`
  - `IsWeChat`
  - `IsWeibo`
- 安全：
  - `IsPassword`

适用场景：

- 注册/登录表单校验
- 后台参数校验
- 用户数据合法性审查
- 文本格式识别

---

### 3.4 crypto 包：加密与哈希

职责：提供常见的安全工具函数，用于密码、数据摘要和 RSA 加解密。

主要能力：

- 哈希：
  - `HashSHA256`
  - `Md5`
- AES：
  - `AES` 相关加解密支持（文件中存在 `aes.go` 与测试）
- RSA：
  - `GenerateRSAKeys()` 生成公私钥
  - `EncryptRSA()` 公钥加密
  - `DecryptRSA()` 私钥解密

适用场景：

- 密码摘要
- 敏感字段加密传输
- 数据安全存储与解密

---

### 3.5 rand 包：随机数、随机字符串和概率函数

职责：生成随机数、随机字符串、随机概率执行判断和英文名。

主要能力：

- 随机数：
  - `Rand6()`：生成 6 位随机数字字符串
  - `Rand4()`：生成 4 位随机数字字符串
  - `Intn`、`Int31n`、`Int63n`
  - `RandInt`、`RandIntM`
- 随机字符串：
  - `NewLenChars(length)`
  - `RandomString(length)`
- 概率执行：
  - `RateToExec(rate)`
  - `RateToExecWan(rate)`
  - `RateToExecWithIn(rate, max)`
- 英文名：
  - `RandEnName()`
  - `RandEnMenName()`
  - `RandEnWomenName()`

适用场景：

- 验证码生成
- 随机账号/密码生成
- 流量控制和概率实验
- 测试数据生成

---

### 3.6 id 包：唯一标识生成

职责：生成分布式 ID 和 UUID。

主要能力：

- `GetSnowflakeId()`：通过 Snowflake 算法生成全局唯一 ID
- `InitSnowflake(node int)`：初始化雪花节点
- `GetToken()`：生成 UUID v4 形式的 Token

适用场景：

- 数据库主键
- 日志和消息追踪
- 业务唯一 Token / 会话标识

---

### 3.7 time 包：时间处理封装

职责：封装常用时间能力，基于 `carbon` 库提供更简洁的时间计算和格式化入口。

主要能力：

- `NowCarbon()` / `NowTime()`：获取当前时间
- `NowUnix()` / `NowUnixMilli()` / `NowUnixNano()`：获取时间戳
- `NowAddSeconds` / `NowAddMinutes` / `NowAddHours` / `NowAddDays` / `NowAddMonths`
- `StrToCarbon()`：字符串转 `carbon.Carbon`
- `StartOfDay` / `EndOfDay`：获取当天起止时间
- `TodayRange()` / `YdayRange()` / `PreMonthRange()`：获取常见时间范围
- `CarbonToDate()` / `CarbonToDateTime()`：时间格式化输出

适用场景：

- 日志时间处理
- 订单/报表时间范围计算
- 任务和活动的时间节点控制

---

## 4. 依赖概览

项目使用的主要第三方依赖包括：

- `github.com/bwmarrin/snowflake`：Snowflake ID
- `github.com/dromara/carbon/v2`：时间处理
- `github.com/satori/go.uuid`：UUID
- `github.com/stretchr/testify`：测试断言
- `github.com/yelinaung/eng-name`：英文名生成
- `golang.org/x/crypto`：加密库

---

## 5. 适用场景总结

整个库适合用于以下通用开发场景：

- Web/API 请求参数转换与校验
- 用户信息字段合法性处理
- 敏感数据加密与摘要
- 业务主键和 Token 生成
- 随机验证码、测试数据、概率行为控制
- 时间范围、日期计算和时间格式化

简言之，这个项目属于“Go 常用基础工具库”，偏向通用型辅助函数，不属于业务系统框架，而是用于提高业务实现效率和统一工具能力。
